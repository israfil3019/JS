// ! if
const day = 'monday';
let sessionInfo = '';
if (day === 'monday') {
  sessionInfo = 'There is single session';
} else if (day === 'wednesday') {
  sessionInfo = 'There is single session';
} else if (day === 'thursday') {
  sessionInfo = 'There is single session';
} else if (day === 'saturday') {
  sessionInfo = 'There is single session';
}
// ! switch case
